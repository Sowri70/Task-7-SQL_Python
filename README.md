I should say this is an interesting task to start with.  

The task involved :

1)    Creation of Database in SQLite3 (within Jupyter Note Book - Python)
2)    Datebase name :   'sales_data'
3)    Table name    :   'sales'
4)    Columns       :    product_id INTEGER,
                         product_name TEXT,
                         order_date TEXT,
                         qty INTEGER,
                         price REAL,
                         sales REAL
  
6)    SUM and COUNT aggregations were used in queries.
7)    GROUP BY and ORDER BY were used to group and filter records
8)    Created Bar and Stack Bar Charts were created for 'Total Sales per product'
9)    Line Chart was made to highlight daily sales

Challenges :

1)    Found it difficult to save the file in pdf format and I kept getting errors from server side.
2)    I could find a way to resolve this issue by saving it on .html format and then converting into pdf, which was successful.

